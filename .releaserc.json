{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    ["@semantic-release/exec", {
      "prepareCmd": "sed -i 's/\\* Version:      [0-9.]*/\\* Version:      ${nextRelease.version}/' src/um-configurator.php && sed -i 's/\"version\": \"[0-9.]*\"/\"version\": \"${nextRelease.version}\"/' package.json && npm run build"
    }],
    ["@semantic-release/git", {
      "assets": ["src/um-configurator.php", "package.json"],
      "message": "feat: ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
    }],
    ["@semantic-release/github", {
      "assets": "dist/um-configurator.zip"
    }]
  ]
}
